// GENERATED by `norns generate` from specs/catalog.t — do not edit.

import { compileWhere } from '@human-synthesis/norns/server'

import { Product } from './schema.c'

export ProductPolicy := {
	entity: Product,
	read: {
		check: (row, user) => !!user?.roles?.includes("admin"),
		where: (ops, user) => compileWhere({"role":"admin"}, { table: Product, ops, user })
	},
	write: {
		check: (row, user) => !!user?.roles?.includes("admin"),
		where: (ops, user) => compileWhere({"role":"admin"}, { table: Product, ops, user })
	}
}
