// GENERATED by `norns generate` from specs/catalog.t — do not edit.
import { sqliteTable, text } from 'drizzle-orm/sqlite-core'
import * as v from 'valibot'

export Product := sqliteTable('catalog_product', {
	id: text('id').primaryKey(),
	title: text('title').notNull()
})

export ProductSchema := v.strictObject({
	id: v.string(),
	title: v.pipe(v.string(), v.maxLength(10000))
})

export ProductInput := v.omit(ProductSchema, ['id'])
